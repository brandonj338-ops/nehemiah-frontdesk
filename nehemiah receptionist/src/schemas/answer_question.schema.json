{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "answer_question",
  "description": "Answer a general/receptionist question. POST /api/receptionist",
  "type": "object",
  "required": ["question"],
  "properties": {
    "question": { "type": "string" },
    "language": { "type": "string", "enum": ["en", "es"], "default": "en" }
  }
}
